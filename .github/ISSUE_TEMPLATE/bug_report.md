---
name: Bug report
about: A reproducer for unexpected behaviour
title: "[bug] "
labels: bug
---

## What happened

## What you expected

## Reproducer

```bash
# minimal command line
```

```yaml
# config used (paste the YAML or attach the file)
```

## Environment

- nsa-eval version (`nsa-eval --version`):
- Python version (`python3 --version`):
- torch version (`python3 -c "import torch; print(torch.__version__)"`):
- Device: cuda / mps / cpu
- If MPS, run `system_profiler SPHardwareDataType | grep "Chip"` and paste the result here.

## Logs / traceback

```
paste here
```

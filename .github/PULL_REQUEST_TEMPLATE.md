## Summary

## Test plan

- [ ] `ruff check .` clean
- [ ] `pytest` green locally
- [ ] If touching `src/nsa_eval/attention/`: numeric regression check updated/added in `tests/test_attention/`
- [ ] If touching a benchmark adapter: a `benchmarks/results/<date>/<slug>.json` manifest is attached

## Notes
